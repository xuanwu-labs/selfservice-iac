# 03-平台代码与脚本目录

> 对应 design.md `04 D2/D7`、config 产物规整约定（specs/docs/scripts 命名）的落地说明。承载 `server/` 代码组织、可插拔适配器接口、`scripts/` 约定。
>
> 📌 **路径演进已落地**：D2 原设想的"Terramate 主仓库内 `platform/` 子目录"已演变为独立 monorepo `selfservice-iac/server/`（独立 go module）。业务包从 `internal/` 提升为 `core/` 顶层（package-by-feature）。本文档已全面对齐实际代码结构。

## 1. `server/` 目录结构（package-by-feature，业务域顶层）

> 权威物理路径，与 `server/AGENTS.md` §目录结构 一致。`core/` 是领域核心（业务逻辑），`data/` 是数据访问层，`internal/` 仅装私有基建，`pkg/db/` 是 sqlc 输入输出。

```
server/
├── cmd/{server,aether,migrate}/   # 二进制入口（HTTP server / CLI / 迁移 runner）
│   ├── server/                      # 平台主二进制（API + Orchestrator）
│   ├── aether/                      # 平台 CLI（原 cmd/tm）
│   └── migrate/                     # 迁移 runner（go:embed migrations/*.sql）
│
├── api/{http,connect}/            # 传输层 handler（契约在仓库根 contracts/）
│   ├── connect/                     # Connect-RPC handler（Server/Admin/Internal 三层）
│   └── http/                        # HTTP 路由、中间件
│
├── core/<domain>/                 # ★ 领域核心（顶层一等公民，package-by-feature）
│   ├── registry/                    #   模块注册、变量契约提取（纯 scalar，D25）
│   ├── catalog/                     #   服务目录、表单 schema、默认值、可见性
│   ├── tenancy/                     #   团队/项目组/space/归属
│   ├── stackmodel/                  #   分层模型、层规则集版本化与迁移（D24/D26）
│   │   ├── pathgenerator/           #     PathGenerator（path_template 渲染）
│   │   ├── granularity/             #     StackGranularity（per-component/per-space/...）
│   │   └── dependency/              #     跨层依赖图（单向无环校验）
│   ├── codegen/                     #   表单→Terramate stack + TF 代码生成（D19）
│   │   ├── generator/               #     PathGenerator + CardinalityInjector 协同
│   │   ├── paramresolver/           #     9 阶段参数解析管道（S1-S9，D28）
│   │   ├── tagmerger/               #     Tag 来源 7 层合并（L1-L7，D28）
│   │   └── provenance/              #     ProvenanceWriter（resolved_params_json 审计）
│   ├── orchestrator/                #   工单状态机、流水线调度
│   ├── approval/                    #   审批引擎（YAML DSL、双门禁 D21）
│   ├── executor/                    #   执行运行时（process/container/k8s/remote，D20）
│   ├── terramate/                   #   TerramateAdapter（exec 调用 terramate CLI，D1）
│   ├── drift/                       #   漂移检测器、plan 解析、调度
│   ├── workspace/                   #   工作仓库 git 治理（go-git）、worktree、重启恢复
│   ├── envtenant/                   #   环境与租户管理（D27，权威见 docs/07）
│   ├── cmdb/                        #   CMDB ingester（state → resources 表）
│   ├── finops/                      #   FinOps 双成本源、预算治理、孤儿检测
│   ├── cloudcreds/                  #   云账号/凭据/IAM 角色模板/OIDC 联邦（D23）
│   ├── identity/                    #   OIDC 认证、SCIM/飞书/钉钉同步（D10）
│   ├── importer/                    #   存量导入（Terraform 1.5+ import 块，D14）
│   ├── gate/                        #   CICD 门禁（阻塞/回调 API、tm gate，D16）
│   ├── ai/                          #   平台 CLI/AI（tm、MCP server、skills，D17）
│   ├── events/                      #   事件总线、Webhook 分发
│   ├── audit/                       #   审计日志写入（human/ai/system actor）
│   ├── adapters/                    #   可插拔适配器接口 + noop 默认实现（D7）
│   │   ├── git/                       #     GitProvider
│   │   ├── cloud/                     #     CloudProvider
│   │   ├── state/                     #     StateBackend（S3/MinIO）
│   │   ├── policy/                    #     PolicyEngine（OPA）
│   │   ├── cost/                      #     CostEstimator（Infracost）
│   │   └── notify/                    #     Notifier（Webhook/IM/邮件）
│   ├── clock/                       #   时钟抽象（测试用 FakeClock）
│   ├── crypto/                      #   加密工具
│   ├── queue/                       #   异步任务队列（River）
│   └── store/                       #   ~~已废弃~~ → 数据访问移至 server/data/repo/
│
├── data/                          # ★ 数据访问层（混合范式：ferret Repo × DIP × sqlc）
│   ├── data.go                      #   pgxpool provider + wire ProviderSet（注册所有 Repo）
│   ├── query_wrapper.go             #   动态查询构造器（MyBatis-Plus 风，pgx + squirrel）
│   ├── repo/                        #   ★ Repo struct（每实体一个，薄包装 *generated.Queries）
│   │   ├── team.go                    #     TeamRepo
│   │   ├── module.go                  #     ModuleRepo（+ CreateWithVersion 跨表事务）
│   │   ├── catalog.go                 #     CatalogRepo（+ ListByDynamicFilter 动态查询）
│   │   ├── stack.go                   #     StackRepo
│   │   └── ...（15 个核心实体）
│   └── dbset/                       #   多表组合 struct（opt-in，如 StackWithSpace）
│
├── internal/                      # 私有基建（不外露，不装业务逻辑）
│   ├── config/                      #   配置加载（DB/OTEL/adapter 等）
│   ├── otel/                        #   OpenTelemetry instrumentation
│   ├── model/                       #   entity 层（opt-in，仅 sqlc model 不够时）
│   ├── proto/                       #   生成的 proto Go 代码
│   ├── mapping/                     #   proto ↔ sqlc model/entity 转换
│   ├── middleware/                  #   HTTP/Connect 中间件
│   ├── auth/                        #   鉴权、RBAC（role_bindings 评估）
│   ├── audit/                       #   D1 guard test（AST 扫描，确保不 import terramate）
│   ├── cli/                         #   CLI 框架（cobra）
│   ├── cmdutil/                     #   CLI 工具函数
│   ├── errors/                      #   错误码定义
│   ├── event/                       #   事件类型定义
│   ├── httpclient/                  #   HTTP 客户端
│   ├── server/                      #   server 启动装配
│   └── utils/                       #   工具函数（snowflake ID 等）
│
├── pkg/db/                        # sqlc 输入输出（可复用数据层）
│   ├── queries/                     #   SQL 真相源（.sql 文件，每表一个）
│   ├── generated/                   #   sqlc 输出（models.go + querier.go + *.sql.go）
│   ├── testdb.go                    #   testcontainers + migrate up/down
│   └── sqlc.yaml                    #   sqlc 配置
│
├── cmd/migrate/migrations/        # goose 迁移（go:embed）
└── contracts/ → ../../contracts/  # proto 契约（仓库根，server 通过 replace 引用）
```

**与 Terramate 的边界**（D1，exec 适配器，物理隔离）：
- `selfservice-iac/` 是独立 monorepo，与 Terramate 仓库物理隔离（不再是同仓库子目录）。
- 平台通过 `server/core/terramate` 的 exec 适配器调用 terramate 二进制（`exec.CommandContext`），**不 import** `github.com/terramate-io/terramate/<任何子包>`。
- D1 guard test（`server/internal/audit/d1_guard_test.go`）AST 扫描 `server/**/*.go`，编译期守护此边界。

## 2. 可插拔适配器接口（Go 伪代码）

```go
// server/core/adapters/git/git.go
type GitProvider interface {
    Clone(ctx, remote, into string) (Repo, error)
    Open(path string) (Repo, error)
}
type Repo interface {
    Fetch(ctx) error
    Checkout(ctx, ref string) error
    WorktreeAdd(ctx, path, branch string) (Worktree, error)
    Commit(ctx, msg string, paths ...string) (sha string, err error)
    Push(ctx) error
    HeadCommit() string
}

// server/core/adapters/state/state.go
type StateBackend interface {
    ListKeys(ctx, prefix string) ([]string, error)
    Lock(ctx, key string) (LockID, error)
    Unlock(ctx, key string, id LockID) error
}

// server/core/adapters/policy/policy.go
type PolicyEngine interface {
    // 对 plan JSON 做策略评估，返回 deny 规则
    Evaluate(ctx, planJSON []byte, input map[string]any) (Decision, error)
}

// server/core/adapters/cost/cost.go
type CostEstimator interface {
    Estimate(ctx, planJSON []byte) (CostReport, error)
}

// server/core/adapters/notify/notify.go
type Notifier interface {
    Notify(ctx, event Event) error
}

// server/core/terramate/terramate.go
type TerramateAdapter interface {
    Run(ctx, dir string, args ...string) (Result, error)   // exec terramate
    Generate(ctx, dir string) (Result, error)
}
```

每个接口有默认实现，可通过 `adapters_config`（DB）按 `kind` 切换实现。

## 3. `frontend/`（自研轻量）

```
frontend/
├── src/
│   ├── pages/        # 目录、申请、审批、漂移、历史
│   ├── components/   # 表单引擎（基于 form_schema_json 渲染）
│   ├── api/          # 调用 platform API
│   └── rbac/         # 前端可见性控制
└── ...
```
- 表单引擎由后端 `catalog_items.form_schema_json` 驱动渲染（声明式），前端不硬编码表单。

## 4. `scripts/` 约定（对应 config 产物规整）

本 change 的脚本产出存放于 `openspec/changes/iac-self-service-server/scripts/`，按**面向功能**分子目录：

```
scripts/
├── 初始化/          # 平台首次部署：DB 初始化、workspace 注册、默认适配器配置
├── 迁移/            # schema migration 脚本（配合 db/migrations）
├── 模块注册/        # 批量注册原子模块、契约提取辅助脚本
├── 编排执行/        # 触发流水线、对账、worktree 清理的工具脚本
└── 状态同步/        # state key 对账、漂移手动触发、同步策略执行辅助
```

- 脚本以功能聚合，命名中文，与 docs 形成对应。
- 实际脚本在 `apply` 阶段（`/opsx:apply`）随实现逐步产出；本 propose 阶段只确立约定。

## 5. 端到端测试落点

- `e2etests/server/`（新增）：覆盖"表单 → 工单 → plan → 断言生成代码与 state key"。
- 复用 `e2etests/` 既有基础设施（script runner、断言），与 Terramate 主仓库 e2e 共存不冲突。
